/**
 * 履歴要約用のGPTプロンプトテンプレート
 */

/**
 * 履歴要約用のシステムプロンプト
 */
export const HISTORY_SUMMARY_SYSTEM_PROMPT = `あなたは日記要約の専門家です。過去7日間の日記投稿を分析し、感情傾向・主要テーマ・思考パターン・成長ポイントを150文字程度で簡潔に要約してください。

要約には以下の要素を含めてください：
- 主な感情傾向（ポジティブ/ネガティブ/変化）
- 繰り返し言及されるテーマや関心事
- 行動パターンや思考の特徴
- 成長や変化の兆し

簡潔で具体的な要約を作成してください。`;

/**
 * 履歴要約用のユーザープロンプトを生成
 */
export function generateSummaryPrompt(entriesText: string): string {
  return `以下の過去7日間の日記投稿を要約してください：

${entriesText}`;
}